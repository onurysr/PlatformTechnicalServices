@model ResetPasswordViewModel

@{
    ViewBag.Title = "Şifre Güncelleme Sayfası";
}


<div class="content">
    <div class="container">
        <div class="row">
            <div class="col-md-12 col-lg-12 contents">
                <div class="row justify-content-center">
                    <div class="col-md-8">
                        <div class="mb-4">
                            <h1 style="text-align:center">@ViewBag.Title</h1>
                        </div>
                        <form asp-controller="Account" asp-action="ConfirmResetPassword" method="post">
                            <input type="hidden" value="@ViewBag.Code" id="Code" name="Code" />
                            <input type="hidden" value="@ViewBag.UserId" id="UserId" name="UserId" />
                            <div class="form-group last mb-4">
                                <label asp-for="NewPassword">Yeni Şifre</label>
                                <input asp-for="NewPassword" class="form-control" />
                                <span asp-validation-for="NewPassword" class="text-danger"></span>

                            </div>

                            <div class="form-group last mb-4">
                                <label asp-for="ConfirmNewPassword">Şifre Onay</label>
                                <input asp-for="ConfirmNewPassword" class="form-control" />
                                <span asp-validation-for="ConfirmNewPassword" class="text-danger"></span>

                            </div>

                            <input type="submit" value="Güncelle" class="btn text-white btn-block btn-primary">
                        </form>
                    </div>
                </div>

            </div>

        </div>
    </div>
</div>